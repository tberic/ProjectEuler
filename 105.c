#include <stdio.h>

int main()
{
    FILE *in = fopen("105.txt", "rt");

    for (i = 0; i < 100; ++i)
    {
        for (j = 0; j < 
        
        
    }

    
        
    
    system("pause");
    fclose(in);
    return 0;
}
